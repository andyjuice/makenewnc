# Copy to `.env` for local live Instagram API testing (`npm run dev`).
# Never commit `.env` — it is gitignored.
#
# Get these values: docs/instagram-setup.md

INSTAGRAM_ACCESS_TOKEN=
INSTAGRAM_USER_ID=
